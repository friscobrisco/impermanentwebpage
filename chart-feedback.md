# Chart Feedback — Coauthor Review

## 1. Sort models by average performance
Currently models are displayed in alphabetical order. Sort them by average performance so the best-performing models appear first in both the chart legend and dataset rendering.

**Status**: To do

## 2. Sort legend by performance
Related to (1) — ensure the legend order reflects performance ranking, making the color assignments easier to interpret.

**Status**: To do

## 3. Show "Average Rank ↓" on y-axis
Add a directional arrow to the y-axis label to make it clear that lower rank is better. Currently the label just says "Average Rank" without the indicator.

**Status**: To do

## 4. Add model name labels at end of lines
Show model names as text labels at the end of each line (after the last data point), making it easier to identify models without relying solely on the legend or hover tooltips.

**Status**: To do

## ~~5. Different line styles per model category~~
~~Use solid/dashed/dotted lines to distinguish statistical models from foundational models.~~

**Status**: Not moving forward with this
